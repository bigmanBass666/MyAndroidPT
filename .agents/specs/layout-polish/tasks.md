# Tasks
- [ ] Task 1: dimen token 引用到所有布局文件（P0-1）
  - 将所有布局中的 padding="16dp" → @dimen/page_padding
  - 将所有 marginTop="20dp"/"12dp" 等 → @dimen/form_field_spacing
  - 将 FAB margin、Card margin 等 → 对应 dimen token
  - `activity_main.xml`, `activity_register.xml`, `activity_welcome.xml`, `activity_todo_list.xml`, `activity_todo_edit.xml`, `activity_todo_detail.xml`, `item_todo.xml`
- [ ] Task 2: Loading 态覆盖（P0-2）
  - MainActivity: btn_login disabled + text change during login
  - RegisterActivity: btn_register disabled + text change during register
  - TodoListActivity: add loading indicator during data load
  - TodoEditActivity: btn_save disabled + text change during save
  - TodoDetailActivity: loading indicator during todo load
  - WelcomeActivity: loading indicator during dashboard data load
- [ ] Task 3: Shape token 统一引用（P0-3）
  - 将所有 layout 中的 `app:cardCornerRadius="12dp"` → `style="@style/ShapeMedium"` 或 `app:shapeAppearance="?attr/shapeAppearanceCornerMedium"`
- [ ] Task 4: 详情页容器净化（P0-4）
  - 将 activity_todo_detail.xml 的三级嵌套（ScrollView > CardView > tinted block）简化为二级
  - 修复 padding 叠加（8dp+16dp=24dp）问题
- [ ] Task 5: Dark 主题（P1-5）
  - 创建 res/values-night/themes.xml，定义 dark theme 色值
  - colors.xml 中补充深色 surface 色值
- [ ] Task 6: Welcome 按钮层级区分（P1-6）
  - btn_quick_add: 保持 FilledButton（主要操作）
  - btn_todo_list: 降级为 OutlinedButton（次要操作）
  - btn_logout: 保持 OutlinedButton + 红色（安全操作）
- [ ] Task 7: 按钮 disabled 态（P1-8）
  - MainActivity: btn_login 在登录过程中禁用
  - RegisterActivity: btn_register 在注册过程中禁用
- [ ] Task 8: 详情页标题 textAppearance（P2-13）
  - 将 `android:textSize="22sp"` `android:textStyle="bold"` → `?attr/textAppearanceTitleLarge`
- [ ] Task 9: RecyclerView padding 冲突（P2-12）
  - 移除 `android:padding="8dp"`，只保留 start/end
- [ ] Task 10: 回归测试
  - 编译 APK (assembleDebug)
  - 安装到模拟器
  - 遍历所有 Activity 的完整功能流程
  - 分析 logcat 日志
  - 输出审计结果

# Task Dependencies
- [Task 1-9] 无依赖关系，可并行执行
- [Task 10] 依赖 [Task 1-9] 全部完成